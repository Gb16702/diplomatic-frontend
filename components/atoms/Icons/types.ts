import type { ReactNode } from "react";

export type IconWrapperProps = {
    size?: string | number;
    fillColor?: string;
    strokeColor?: string;
    strokeWidth?: string | number;
    className?: string;
    children?: ReactNode;
};
